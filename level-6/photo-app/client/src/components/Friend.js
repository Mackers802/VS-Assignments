import React from 'react'

export const Friend = (props) => {

    const { val } = props
    return (
        <div className="friendsList">
                    <button > 
                      {val}
                    </button>
                  </div>
    )
}
